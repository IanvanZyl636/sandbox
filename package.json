{
  "name": "sandbox",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "tsc && sass styles.scss dist/out-tsc/styles.css && serve",
    "build": "tsc && sass --watch styles.scss dist/out-tsc/styles.css",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "sass": "^1.44.0",
    "typescript": "^4.5.2"
  },
  "devDependencies": {
    "serve": "^13.0.2"
  }
}
